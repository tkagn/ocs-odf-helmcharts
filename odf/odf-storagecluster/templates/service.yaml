apiVersion: v1
kind: Service
metadata:
  name: {{ include "odf-storagecluster.fullname" . }}
  labels:
    {{- include "odf-storagecluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odf-storagecluster.selectorLabels" . | nindent 4 }}
