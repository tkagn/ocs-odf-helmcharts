apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odf-storagecluster.fullname" . }}-test-connection"
  labels:
    {{- include "odf-storagecluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odf-storagecluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
