apiVersion: v1
kind: Service
metadata:
  name: {{ include "odf.fullname" . }}
  labels:
    {{- include "odf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odf.selectorLabels" . | nindent 4 }}
